export interface FormTextInputProps {
  name: string;
  label: string;
  placeholder?: string;
  description?: string;
  disabled?: boolean;
}

export interface AuthHeaderProps {
  title: string;
  description: string;
}
